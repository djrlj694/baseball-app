[tool.poetry]
name = "alembic"
version = "1.0.0"
description = "Database migrations for the backend."

[tool.poetry.dependencies]
python = ">=3.10,<4.0"
alembic = "^1.13.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
